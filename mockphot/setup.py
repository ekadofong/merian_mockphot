import setuptools

setuptools.setup (
    name = 'merian_mockphot',
    version='0.0.1',
    author='Erin Kado-Fong',
    description='Generate mock photometry for Merian I/O tests',
    packages=setuptools.find_packages(),
    python_requires='>=3.7'
    )
